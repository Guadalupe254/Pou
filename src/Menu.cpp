#include "../include/Menu.hpp"

Menu::Menu() {}

void Menu::draw(sf::RenderWindow& window) {
    // Dibujar menú (placeholder)
}

int Menu::handleEvent(sf::Event event) {
    // Manejar eventos del menú (placeholder)
    return 0;
}
